const Card = ({ img, text }) => {
  return (
    <div
      style={{
        width: "40vh",
        height: "60vh",
        backgroundColor: "white",
        borderRadius: "15px",
      }}
    ></div>
  );
};

export default Card;
